# Smart Draft App

Resume builder web app.

## Local development

- Install dependencies: `npm install`
- Start dev server: `npm run dev`
- Production build: `npm run build`

## Features

- Template selection + accent colors
- Mobile-friendly builder + preview
- Import resume from PDF upload or pasted text
